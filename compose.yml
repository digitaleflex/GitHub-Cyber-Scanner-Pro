services:
  cyber-scanner:
    build: .
    container_name: cyber_github_scanner
    restart: always
    ports:
      - "8000:8000"
    volumes:
      - ./data:/app/data
      - ./reports:/app/reports
