version: '3.8'
services:
  cyber-scanner:
    build: .
    container_name: cyber_github_scanner
    restart: always
    ports:
      - "8000:8000"
    env_file: .env
    volumes:
      - ./data:/app/data
