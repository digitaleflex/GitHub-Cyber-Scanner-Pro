# Extensions (gh extension)

List extensions:

```bash
gh extension list
```

Browse available extensions:

```bash
gh extension browse
```

Install/remove (confirm first):

```bash
gh extension install OWNER/REPO
gh extension remove <name>
```
