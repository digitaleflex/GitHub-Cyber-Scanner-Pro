# Search (gh search)

Repo search:

```bash
gh search repos "<query>"
```

Issue search:

```bash
gh search issues "<query>"
```

PR search:

```bash
gh search prs "<query>"
```

Code search:

```bash
gh search code "<query>"
```
